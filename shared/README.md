# Shared

This folder contains framework-neutral design tokens. Each implementation imports the same CSS custom properties so component behavior and styling can evolve consistently across React, Vanilla, Svelte, and Angular.
