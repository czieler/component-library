# Project Instructions

- Keep React and Vanilla implementations framework-appropriate and independently runnable.
- Reuse the CSS custom properties in `shared/design-tokens.css`.
- Keep components small, accessible, and free of unnecessary dependencies.
- Do not add Svelte or Angular until their planned implementation begins.
- Validate changes with the relevant demo build before expanding scope.
